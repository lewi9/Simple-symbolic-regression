#include <stdio.h>
#include <limits.h>
//
int main( int argc, char * argv[] )
{
	if(argc != 2) return INT_MAX;
	int input, result, a, b, c;
	result=a=b=c=0;
	sscanf(argv[1], "%d", &input);
//s
	b = 1 - 1;
	input = result - 0;
	result = a + a;
	b = 1 + 1;
	c = 1 * input;
	result = a + 1;
//e
    return result;
}
