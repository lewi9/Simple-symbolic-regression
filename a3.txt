#include <stdio.h>
#include <limits.h>
//
int main( int argc, char * argv[] )
{
	if(argc != 2) return INT_MAX;
	int input, result, a, b, c;
	result=a=b=c=0;
	sscanf(argv[1], "%d", &input);
//s
	b = input * c;
	input = input - input;
	a = input * 1;
	b = c * a;
	input = input + input;
	c = 1 - result;
//e
    return result;
}
